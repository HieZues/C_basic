#include <stdio.h>
double pi = 3.14;
int main() {
    double r;
    printf ("nhap vao ban kinh r: ");
    scanf("%lf", &r);
    printf("Dien tich: %lf\n", 4 * pi * r * r);
    printf("The tich: %lf\n", (4.0 / 3.0) * pi * r * r * r);
    return 0;
}
