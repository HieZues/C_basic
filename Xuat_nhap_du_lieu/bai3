#include <stdio.h>
#include <math.h>

int main (){
	int a, b;
	printf ("nhap vao a: ");
	scanf ("%d", &a);
	printf ("nhap so b: ");
	scanf ("%d", &b);
	printf ("a + b = %d\n", a + b);
	printf ("a - b = %d\n", a - b);
	printf ("a * b = %d\n", a * b);
	printf ("a / b = %d\n", a / b);
	return 0;
}
